global:
  resolve_timeout: 5m

route:
  group_by: ['alertname', 'severity']
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 12h
  receiver: 'email-notifications'

receivers:
  - name: 'email-notifications'
    email_configs:
      - to: 'nessaayusafitri2219@gmail.com'
        from: 'alertmanager@mlops-project.com'
        smarthost: 'smtp.gmail.com:587'
        auth_username: 'nessaayusafitri2219@gmail.com'
        auth_password: 'your-app-password'
        headers:
          Subject: 'MLOps Alert: {{ .GroupLabels.alertname }}'

inhibit_rules:
  - source_match:
      severity: 'critical'
    target_match:
      severity: 'warning'
    equal: ['alertname']
